{
  "name": "rel-enc",
  "description": "Relativo a encuestas",
  "version": "0.1.48",
  "author": "Codenautas <codenautas@googlegroups.com>",
  "license": "MIT",
  "main": "lib/server/rel-enc.js",
  "types": "lib/server/rel-enc.d.ts",
  "files": [
    "lib",
    "src/client"
  ],
  "dependencies": {
    "@types/node": "~10.3.0",
    "backend-plus": "~0.21.5",
    "js-to-html": "~1.0.3",
    "types.d.ts": "~0.1.25"
  },
  "devDependencies": {},
  "engines": {
    "node": ">= 8.0.0"
  },
  "scripts": {
    "prepublish": "tsc || echo \"continue w/error\" && tsc -p src/client || echo \"continue w/error\"",
    "xprepublish": "tsc && tsc -p src/client",
    "test": "mocha --reporter spec --bail --check-leaks test/",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --reporter dot --check-leaks test/",
    "start": "node server/server-rel-enc.js"
  },
  "qa-control": {
    "package-version": "0.3.0",
    "coverage": 90,
    "run-in": "server",
    "stability": "extending",
    "type": "lib",
    "ecmaVersion": 6
  }
}
